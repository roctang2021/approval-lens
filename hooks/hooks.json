{
  "description": "Permission Lens — annotates Bash permission dialogs with a plain-language risk explanation. Never returns a decision; the native dialog always shows.",
  "hooks": {
    "PermissionRequest": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --quiet \"${CLAUDE_PLUGIN_ROOT}\"/hooks/permission_lens.py || true",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
