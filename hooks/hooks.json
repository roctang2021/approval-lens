{
  "description": "Permission Lens — puts a plain-language risk explanation on the permission dialog for risky Bash/WebFetch/Write/Edit calls (permissionDecision \"ask\" + reason). Never returns allow/deny; below the ask threshold it prints {} and native behavior is untouched.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|WebFetch|Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v uv >/dev/null 2>&1 || PATH=\"$HOME/.local/bin:$HOME/.cargo/bin:/opt/homebrew/bin:/usr/local/bin:$PATH\"; uv run --quiet \"${CLAUDE_PLUGIN_ROOT}\"/hooks/permission_lens.py' || true",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
